# Copiar a ".env" en el VPS y completar con valores reales.
# ".env" NUNCA debe commitearse (ya esta en .gitignore).

OPENAI_API_KEY=

# Puerto del host donde queda expuesta la UI (nginx). Default: 80.
UI_PORT=80
