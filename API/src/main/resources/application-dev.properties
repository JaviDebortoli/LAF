# Development profile overrides
# Activate with: SPRING_PROFILES_ACTIVE=dev

# Disable external LLM call by default in local dev to avoid dependency on external API keys.
laf.narration.llm.enabled=false
