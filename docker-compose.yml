services:
  api:
    build:
      context: ./API
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    expose:
      - "8080"
    restart: unless-stopped

  ui:
    build:
      context: ./UI
    ports:
      - "${UI_PORT:-80}:80"
    depends_on:
      - api
    restart: unless-stopped
